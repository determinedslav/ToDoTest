//api url
const apiURL = "https://auto.loanvantage360.com/fps/api/";

//api end points
const getAll = apiURL + "todo";
const get = apiURL + "todo/";
const post = apiURL + "todo";
const put = apiURL + "todo";
const del = apiURL + "todo/";

const endPoints = {
    getAll,
    get,
    post,
    put,
    del
}

export default endPoints